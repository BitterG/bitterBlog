---
title: "native主动调用"
date: "2026-05-15"
updated: "2026-05-15T01:38:57.299Z"
category: "lsposed"
tags: ["LSPosed", "Android", "逆向"]
---

# native 主动调用

```cpp
// JNI函数类型定义 IDA反编译查看
typedef jdouble (*jni_func_t)(JNIEnv *, jclass, jdouble);

extern "C" JNIEXPORT jdouble JNICALL
Java_com_example_mylsposedmod_HookMain_nativeCallSquareRoot(
JNIEnv *env, jobject thiz, jdouble value) {
    // 这里明明已经加载了so但是无法找到？
    //最终通过on_library_loaded中加载目标so时进行句柄保存
    // void on_library_loaded(const char *name, void *handle) {
    //     if (ends_with(name, "libnative-lib.so")) {
    //         lib_handle = handle; // 保存对应so句柄
    //         void *target = dlsym(handle, "Java_com_example_myapplication_Java2CppTest_calculateSquareRoot");
        
    //         hook_func(target, (void *) fake_calculateSquareRoot, (void **) &back_calculateSquareRoot);
    //     }
    // }
    
    // void* handle = dlopen("libnative-lib.so", RTLD_NOW); 无法找到
    if (!lib_handle) {
        LOGI("dlopen failed: %s", dlerror());
        return 888.8;
    }
    //查找目标方法
    jni_func_t calculateSquareRoot = (jni_func_t)dlsym(lib_handle, "Java_com_example_myapplication_Java2CppTest_calculateSquareRoot");
    if (!calculateSquareRoot) {
        LOGI("dlsym failed: %s", dlerror());
        return 888.8;
    }
    //调用并返回
    double result = calculateSquareRoot(env, (jclass)thiz, value);
    LOGI("接受到原始调用参数:%f, 正常返回: %f", value, result);
    //修改最终返回值
    return result + 800000;
}
```

主动调用返回在native hook的后面执行

![图片](/img/lsposed/d04a1aec23e5639b/1756562855855-3699ac94-0921-4d38-b842-994e47ec4df1-423327.png)

注意主动调用时机保证在目标so加载完后再调用

例如

![图片](/img/lsposed/d04a1aec23e5639b/1756563271656-a898e0be-29ee-4313-b632-9e8c4e5569ed-425727.png)

```cpp
#include <jni.h>
#include <string>
#include <dlfcn.h>
#include "head.h"
#include <android/log.h>

#define TAG "HookNativeLib"
#define LOGI(...) __android_log_print(ANDROID_LOG_INFO, TAG, __VA_ARGS__)

static HookFunType hook_func = nullptr;
static void* lib_handle = nullptr;  // 保存库句柄
static void* target_func = nullptr; // 保存函数指针

//int (*backup)();
double (*back_calculateSquareRoot)(double a1);

double fake_calculateSquareRoot(double a1) {
    // 强制返回固定值，或者修改原始逻辑
    double result = back_calculateSquareRoot(a1);
//    __android_log_print(ANDROID_LOG_INFO, "Hook", "Hooked sqrt(%f) = %f", a1, result);
    //修改最后返回值
    return result + 100000;
}

FILE *(*backup_fopen)(const char *filename, const char *mode);

FILE *fake_fopen(const char *filename, const char *mode) {
    if (strstr(filename, "banned")) return nullptr;
    return backup_fopen(filename, mode);
}

jclass (*backup_FindClass)(JNIEnv *env, const char *name);
jclass fake_FindClass(JNIEnv *env, const char *name)
{
    if(!strcmp(name, "dalvik/system/BaseDexClassLoader"))
        return nullptr;
    return backup_FindClass(env, name);
}
static inline bool ends_with(const std::string& str, const std::string& suffix) {
    return str.size() >= suffix.size() &&
           str.compare(str.size() - suffix.size(), suffix.size(), suffix) == 0;
}

void on_library_loaded(const char *name, void *handle) {
    // hooks on `libnative-lib.so`
//    if (std::string(name).ends_with("libnative-lib.so")) {
    if (ends_with(name, "libnative-lib.so")) {
        lib_handle = handle;
        void *target = dlsym(handle, "Java_com_example_myapplication_Java2CppTest_calculateSquareRoot");

        hook_func(target, (void *) fake_calculateSquareRoot, (void **) &back_calculateSquareRoot);
    }
}

extern "C" [[gnu::visibility("default")]] [[gnu::used]]
jint JNI_OnLoad(JavaVM *jvm, void*) {
    JNIEnv *env = nullptr;
    jvm->GetEnv((void **)&env, JNI_VERSION_1_6);
    hook_func((void *)env->functions->FindClass, (void *)fake_FindClass, (void **)&backup_FindClass);
    return JNI_VERSION_1_6;
}

extern "C" [[gnu::visibility("default")]] [[gnu::used]]
NativeOnModuleLoaded native_init(const NativeAPIEntries *entries) {
    hook_func = entries->hook_func;
    // system hooks
    hook_func((void*) fopen, (void*) fake_fopen, (void**) &backup_fopen);
    return on_library_loaded;
}

//主动调用
//typedef double (*func_t)(double);
// JNI函数类型定义
typedef jdouble (*jni_func_t)(JNIEnv *, jclass, jdouble);

extern "C" JNIEXPORT jdouble JNICALL
Java_com_example_mylsposedmod_HookMain_nativeCallSquareRoot(
        JNIEnv *env, jobject thiz, jdouble value) {
//    void* handle = dlopen("libnative-lib.so", RTLD_NOW);
    if (!lib_handle) {
        LOGI("dlopen failed: %s", dlerror());
        return 888.8;
    }
    jni_func_t calculateSquareRoot = (jni_func_t)dlsym(lib_handle, "Java_com_example_myapplication_Java2CppTest_calculateSquareRoot");
    if (!calculateSquareRoot) {
        LOGI("dlsym failed: %s", dlerror());
        return 888.8;
    }
    //调用并返回
    double result = calculateSquareRoot(env, (jclass)thiz, value);
    LOGI("接受到原始调用参数:%f, 正常返回: %f", value, result);
    return result + 800000;
}

```



> 更新: 2025-08-30 22:14:46  
> 原文: <https://www.yuque.com/kugua-4bekq/bp62nf/zu05fth3i7rxhr8l>